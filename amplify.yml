version: 0.1
frontend:
  phases:
    build:
      commands:
        - npm run build
         
    preBuild:
      commands:
        - cd frontend
        - npm install 
  artifacts:
    baseDirectory: ./frontend/build
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*